export enum InviteType {
  ARTIST_TO_SHOP = 'artist_to_shop',
}

export enum InviteReaction {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
}

export enum NotifyType {
  CREATE = 'create',
  UPDATE = 'update',
  DELETE = 'delete',
  ADD = 'add',
}
